{
  "name": "tme-aquarium",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "离线优先的肿瘤微环境机制教学与实验平台",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "test": "node --test",
    "audit": "node scripts/scenario-audit.mjs",
    "check": "node scripts/static-audit.mjs",
    "verify": "npm run evidence && npm test && npm run audit && npm run check",
    "evidence": "node scripts/export-evidence.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
