# v2.0 升级指南

## 从 v1.1.x 升级

直接用 v2.0 文件替换旧发布目录即可。首次导入旧存档时，应用会在内存中完成迁移，然后按 v3 格式重新保存。

## 存档迁移规则

- v1：保留旧癌细胞、T 细胞和环境状态；缺失的巨噬细胞、CAF 与新环境字段由兼容逻辑补齐。
- v2：保留已有巨噬细胞和 CAF；为 T 细胞补充 `stemlike`、`terminalExhaustion`，为环境补充 `chronicInflammation`。
- v3：原样验证并载入。
- 所有迁移都会执行数值范围、数组长度、网格尺寸和最大文件体积检查。

迁移后的新变量是合理初始化值，不是从旧数据“推断出的真实生物状态”。因此不应把跨版本指标差异解释为实验效应。

## 行为变化

- “炎症”图层现在只表示较短程的急性趋化/激活代理。
- 新增“慢性压力”图层；它消退更慢，并参与 T 细胞终末耗竭样状态和巨噬细胞功能轴更新。
- 免疫增强不再简单降低一个耗竭变量，而是对不同状态产生有限、不同程度的影响。
- 巨噬细胞会对局部低氧梯度产生方向偏好。
- CSV 和实验记录导出包含新指标，依赖旧列名的分析脚本需要更新。

## 发布前检查

```bash
npm run verify
```

随后在目标浏览器手动检查：

- 启动、暂停、单步和速度切换；
- 七个地图图层与空间探针；
- 捕获/清除实验基线；
- 四项研究任务；
- v1/v2/v3 存档导入；
- CSV、实验记录和状态导出；
- 色觉友好、减少动效和窄屏布局；
- Service Worker 更新与离线重载。
